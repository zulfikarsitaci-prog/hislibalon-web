backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "images"

collections:
  - name: "magaza"
    label: "Katalog & Ürünler"
    files:
      - label: "Tüm Ürünler"
        name: "urunler"
        file: "urunler.json"
        fields:
          - label: "Ürün Listeniz (Düzenlemek için tıklayın)"
            name: "products"
            widget: "list"
            summary: '🎁 {{fields.title}} — {{fields.price}}'
            collapsed: true
            minimize_collapsed: true
            fields:
              - {label: "Ürün Adı", name: "title", widget: "string"}
              - {label: "Kategori", name: "category", widget: "select", options: ["Çiçekli Aranjmanlar", "Hediyelik & Peluş"]}
              - {label: "Fiyat (Örn: 450 TL)", name: "price", widget: "string", required: false}
              - {label: "Shopier Linki", name: "shopier", widget: "string", required: false}
              - {label: "Açıklama", name: "description", widget: "text"}
              - {label: "Stokta Var Mı?", name: "inStock", widget: "boolean", default: true}
              - {label: "Ana Fotoğraf", name: "image1", widget: "image"}
